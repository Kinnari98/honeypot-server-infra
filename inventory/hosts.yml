---
all:
  children:
    machines:
      hosts:
        napoleon: # name of the machine we are using
          ansible_host: #ip or hostname
          ansible_user: azureuser
          ansible_ssh_private_key_file: /path/to/private/key
          ansible_python_interpreter: /usr/bin/python3