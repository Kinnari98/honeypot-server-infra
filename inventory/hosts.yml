---
## inventory/hosts.yml
## Palvelinlista Ansiblelle. Vain hostien nimet täällä.
## Yhteysparametrit (IP, SSH-avain, käyttäjä) ovat host_vars/<hostname>.yml:ssä,
## joka on gitignored. Malli: host_vars/server-name.yml.example

all:
  children:
    machines:
      hosts:
        hannibal:
